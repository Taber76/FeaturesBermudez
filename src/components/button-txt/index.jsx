import React from "react";

import { View, Text } from "react-native";

import { styles } from "./styles";

const ButtonText = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Button text</Text>
    </View>
  );
};

export default ButtonText;
